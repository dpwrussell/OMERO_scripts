# CycIF Channel CSV to YAML Script

Converts between custom CSV format for Channel metadata and the required YAML
format for using `omero render`.

```bash
python csv2yaml.py <in_csvfile> <out_yamlfile>
```
